import styled from "styled-components";

const WeatherChart = () => {
  return <Chart>test</Chart>;
};

const Chart = styled.section`
  height: 50%;
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
`;

export default WeatherChart;
